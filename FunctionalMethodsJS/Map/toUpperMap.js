var words = ['foo', 'bar', 'baz']

var withMap = words.map(word => word.toUpperCase());

console.log(withMap);








































/////////////////////////

// function toUpper(s) {
//     return s.toUpperCase();
// }

// var withMap = words.map(toUpper);